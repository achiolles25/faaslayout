<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sti.taxation">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FaasFillUpDetalsActivity"></activity>
        <activity android:name=".ApproveAdminActivity" />
        <activity android:name=".FAAS" />
        <activity android:name=".AdminViewApplication" />
        <activity android:name=".AdminPendingView" />
        <activity android:name=".AdminPending" />
        <activity android:name=".Admin" />
        <activity android:name=".TransactionId" />
        <activity android:name=".Application" />
        <activity android:name=".ApplicationFragment" />
        <activity android:name=".Assessor" />
        <activity android:name=".Customer" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>